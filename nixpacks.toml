[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["cd Backend && chmod +x mvnw && ./mvnw clean package -DskipTests"]

[start]
cmd = "cd Backend && java -Dspring.profiles.active=prod -jar target/*.jar"
